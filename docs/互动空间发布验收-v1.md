# 《这一身》互动空间发布验收 v1

更新时间：2026-08-01

## 1. 平台合同

- 创作规范：`reference/platform/interact-creation/SKILL.md`
- 发布规范：`reference/platform/interactive-content-publish/SKILL.md`
- 发布 MCP：`interative_content_mcp`（平台文档中的固定拼写）
- MCP 工具：`query_game_app_list`、`get_upload_token`、`modify_game_app`、`submit_audit_game_app`
- 目标方向：竖屏
- 入口：zip 根目录的 `index.html`
- 包体上限：20 MiB
- 运行基线：iOS Safari 13.4、Android WebView 119

## 2. 当前产物

- 命令：`npm run package`
- 文件：`release/zhe-yi-shen-mvp.zip`
- 独立上传图标：`docs/promo/app-icon-300.png`（300 x 300 PNG，2,527 字节；不进入游戏 zip）
- 图标来源：`gpt-image-2` 专用方图生成，不是封面裁切或本地滤镜；生成证据与 48px 缩小审阅图位于 `output/imagegen/zhe-yi-shen-release-icon-v1/`
- zip 体积：7,808,268 字节（7.45 MiB）
- SHA-256：`85ae8eb902d2a1a3a9a7786d51b975183c4802612dbcbdb6dff513f2c10d6b01`
- 解压内容：8,784,272 字节（8.38 MiB）
- 玩家运行时文件：300 个（zip 目录项不计）
- 平台上限余量：12,187,248 字节（11.62 MiB）
- 内部预算：解压内容不得超过 18 MiB，始终为平台 20 MiB 上限预留至少 2 MiB
- AI 通道：`tt.callAIChatCompletion`
- 模型常量：`doubao-seed-2-0-lite-260428`（出生与后台命运链路统一；平台不做模型映射，此 ID 必须在火山方舟账号内已开通且 Key 具备该模型调用权限，否则 `tt.callAIChatCompletion` 必失败）
- production 音频：原生媒体元素加载包内相对路径，不调用 `fetch` 或 `XMLHttpRequest`；11 条配乐与 9 条章节/房间环境声均由 WAV 母版一次直编为 22.05 kHz、单声道、96 kbps MP3，包装阶段禁止二次有损转码
- production 美术：`art-preload.ts` 先按 PNG 解码像素量显示真实装帧进度，以最多 3 路并发准备公共、首屏和童年正式美术，再动态导入游戏模块；少年章优先、其余章节在游玩时按帧预算后台预热。章节过场结束前再次确认下一章资源完整，任一原件失败即停在可重试错误页，不进入程序化或低清降级画面

production 包不包含以下开发内容：

- `item-art-review.html`
- `voice-review.html`
- 声音来源清单与语音 QA 报告
- 语音概念试听资产
- 六章环境声 WAV 母版（工程内保留用于来源和哈希校验）
- 与正式道具图集重复的标签页 favicon

## 3. 自动验收结果

目录和最终 zip 都必须执行平台校验器，任一 block 错误都会让命令返回非零。

```bash
bash scripts/run-validator.sh --required index.html --max-size 20971520 dist
bash scripts/run-validator.sh --required index.html --max-size 20971520 release/zhe-yi-shen-mvp.zip
```

2026-08-01 结果：

| 门禁 | 结果 |
| --- | --- |
| 必需文件 | 通过 |
| 20 MiB 包体 | 通过 |
| 动态代码 | 通过 |
| 网络请求 | 通过 |
| 页面跳转 | 通过 |
| 外部链接 | 通过 |
| 内联事件 | 通过 |
| XSS 操作 | 通过 |
| 不支持 / 废弃 API | 通过 |
| WebView 浏览器兼容性 | 通过 |
| 18 MiB 内部预算与至少 2 MiB 预留 | 通过（当前预留 12,187,248 字节） |
| 独立发布图标 300 x 300、1 MiB 内、48px 主体可辨 | 通过（2,527 字节） |
| 配乐/环境床单次 96 kbps 编码且发行副本与母版哈希一致 | 通过（11 + 9 条，共 4,160,317 字节） |

内容门禁同步通过：77/77 道具、35/35 弹体、35/35 弹体审计、77/77 Image2 来源消费、45/45 音效/环境/BGM 母版、93/93 固定语音、15 个大小 Boss 阶段形态、41 招、164 帧、12 个命名组合、6 个人生阶段。

成年《响个不停》的固定八通已完成声音闭环：老婆、医院、妈妈、系统提示、父亲、同事与主角末句共 7 个有声节点，第 2 通陌生号码按设计保持静默。留灯间看守人使用 `light-room-left-this` 独立录音。严格语音门禁为 93/93、总时长 372.3 秒；93 条发音 QA 全部通过，没有人工复核、缺失、响度越界或失败资产。

专项运行时审计同步通过：

- 旧发行链会把已编码的配乐与环境床再次压成 24 kbps；对全部曲目按最终解码波形复核，配乐平均 SI-SDR 仅 21.05 dB、环境床仅 15.42 dB，是持续砂化与“像接缝撕裂”的主要内容层来源。现改为 WAV 母版一次直编 96 kbps，分别提高到 48.79 dB 与 33.18 dB；`validate:release-audio` 逐曲检查编码参数与 SHA-256 一致性，阻止二次转码回归。旁白继续沿用用户已确认的 24 kbps 体积策略。
- 九条环境床与紧张层的烘焙淡化会在曲尾 180ms 巡逻跳转前完全收尽；运行时按 WAV 逻辑时长触发，buffered 后端同时显式设置 `loopEnd`，不会把 MP3 容器尾差算进循环体。最终 MP3 逐样本门禁中，十条单元素循环的接缝跳变量最高仅为附近自然采样变化 P99 的 0.35 倍（绝对值最高 0.004148）；Chromium 强制跨缝后两路持续播放且常驻媒体仍只有 2 条。
- 10 首 production BGM 的双元素循环换岗已按烘焙接缝改为线性恒和。实测换岗两路中位相关系数 0.89；旧等功率曲线会把接缝平均抬高 1.6–2.9 dB、最大短窗约 3.6 dB，现在线性换岗把平均偏差控制在约 ±0.7 dB 内，并由 `validate:sound` 阻止等功率写法回归。
- 12 轮 × 9 条平台音频路由共 108 次切换后，语音缓存最多 8、BGM 最多 2、环境声最多 1；静音后所有播放流归零，媒体滤波图保持有界。快速连续切换四首 BGM 同样只保留当前与上一首两路，音乐滑杆归零后播放路数为 0。
- 真实 Chromium 同时播放旁白、BGM、环境床、紧张层并周期触发高强度音效时，共享 limiter 最深衰减约 0.41 dB，中位约 0.006 dB，没有持续压缩泵动。
- 在 BGM 正处于双元素换岗时冻结页面并恢复，旧主播放器会退出，只保留 1 路 BGM、1 路环境声和 1 路紧张层；分轨静音与总静音在回前台后不会复活，重新启用总声音才恢复请求中的三路。
- 宿主音频焦点单独暂停当前旁白后，`focus/pageshow` 与下一次真实手势会从原播放头续播，不再只恢复三条循环轨而让 `activeVoicePriority` 永久堵住候补席；实测播放头由 0.44 秒继续到 1.09 秒，显式 `stopVoice()` 后同样事件不会把旧声复活。
- 嵌入式宿主打开系统浮层等“页面仍可见但窗口短暂失焦”不再误把当前旁白永久截断：最终压缩包中旁白先从 0.506 秒自然播到 0.610 秒，宿主单独暂停后再从 0.623 秒续到 1.275 秒；同时会释放摇杆、命运拖拽、特殊房与暂停页的长按状态，按住“结束本局”失焦并越过 1 秒门槛也不会误结算。真正进入隐藏态时仍会停止旁白或自动暂停，避免后台偷播。
- 战斗/命运牌的暂停改为挂起旁白而非销毁播放头：最终压缩包中在 1.318 秒暂停、继续后播到 1.971 秒；后台自动暂停回前台时仍保持安静，玩家解除暂停后再从 1.972 秒续到 2.621 秒。开发 Web Audio 后端同样在 0.229 秒冻结、恢复后续到 0.677 秒。暂停页临时解除 BGM/环境床闪避，场景结束、静音与显式取消仍走 `stopVoice()`，不会复活旧句。
- 对 `HTMLMediaElement.play()` 永久悬空与迟到成功做故障注入：统一 1.8 秒起播截止，同一元素只保留一个起播事务；旁白三次失败后会释放主持权并继续候补，迟到旧声会再次暂停，BGM/环境床首次悬空会自动重试。真机性能面板同步显示起播超时与迟播拦截计数。
- 同一元素第一次起播在 2.4 秒才兑现、第二次自愈已成功时，旧 Promise 原会反过来暂停健康的新一代播放：旁白、BGM、环境床与紧张层都会显示 active 却实际静音。起播事务现带元素世代号，四轨均在迟到回调后继续播放；截止线前取消的旧旁白/环境床仍保持暂停，不会为修自愈而放出幽灵声。
- 活动旁白在已经开口后注入媒体解码 `error`，旧实现会永久保留主持权并把下一句堵在候补席；现会释放坏元素并立即让候补出列。若错误发生在暂停页内，则保持 0 路偷播，解除暂停后才播放候补。宿主只暂停人声时 BGM 会从 ducked 0.103 恢复到 0.187，旁白再次可闻后才重新闪避。
- 环境床、主 BGM 与紧张层同时注入不可恢复的元素错误后，三轨均会创建新元素恢复，请求状态不丢；坏 BGM 循环替补也会退池并重新预备，下一次跨缝由新替补接管，最终仍只保留 2 路配乐媒体。Web Audio 完全不可用时，元素音效池和场景点声的坏解码器同样会整池/单席释放后重建，常驻量保持 5。
- 以 12 个媒体名额模拟保守 WebView：环境、主 BGM、循环替补、紧张层占 4 格后，旁白缓存硬上限为 8，总驻留量保持 12；童年前八席已改为真正即将触发的台词，不再预载播完的开场漫画。正式旁白起播成功、拒播计数为 0。
- 标题页全音频温启动支持真取消：600ms 慢加载下调用 `stopAudioWarm()` 会同步释放当前临时元素，立即重启仍最多 1 条临时媒体管线；6× CPU 下温启动同步开销约 2–5ms，测量窗口无长任务。
- 章节预热重入故障注入中，同章三次加相邻两章并发原会同时打开 5 条临时解码流；现已改为全局单通道、同章共享任务，峰值为 1，`stopAudioWarm()` 会同步取消当前章节预热并清掉排队旧任务。
- 延迟 4/6 秒的章节音频预热现同时绑定对局号与发起章节；玩家快速过章后，旧定时器不会回来挤占当前章节的单通道预热线。现代 Chromium 的 `decodeAudioData` 会同时完成回调和 Promise，内联音效的全波形起声扫描现用 once 合流，不再在开局重复做两遍。
- 在 Web Audio 完全不可用的降级模式下，六类元素音效与四类场景点声原会把常驻媒体从 12 堆到 30，并触发 13 次解码器拒绝、整句旁白失败；现将元素音效 LRU 限为 4 席、场景点声限为 1 席，并与动态旁白缓存共享 12 席总预算。压力后仍只有 12 条、正式旁白拒播为 0；音效/环境分轨静音会释放可选席位，旁白缓存可从 3 自动恢复到 8。
- 108 次路由切换曾暴露淡化令牌强引用泄漏：播放器与滤波图虽已释放，`fadeTokens` 仍从 0 长到 97。所有媒体退池现统一删除淡化令牌，同样压力下峰值为 3、静音收敛到 2，不再拖住旧 DOM/媒体对象。
- BGM 切换的上一首原本在 2.1 秒淡出后只会暂停和归零，仍带着 `src` 与 Web Audio 图长期占一席；首章稳定态因此始终顶到 12/12。旧主轨现在只在淡出代次仍有效、且未重新成为主轨/循环替补/换岗成员时真正退池；六章稳定态均降到 11 席，A→B→A→晚年→A 的快速重入在迟到回调全部结算后仍只有 `under-bed` 一路主 BGM、总驻留 11。
- buffered 运行时跨六类战斗/剧情路线累计模拟 20 分钟，强制 GC 后堆从 18.24 MiB 到 18.48 MiB（+0.24 MiB）；141 敌人的正式终局怪潮仍约 59–60 FPS，没有弹体、爆发或死亡记录越界。
- production 原生媒体后端跨六类路线累计模拟 24 分钟、84 次重场景切换，强制 GC 后堆从 22.36 MiB 到 22.73 MiB（+0.37 MiB）；媒体峰值 12、旁白缓存 8、候补 4、BGM 2、环境床 1，旁白拒播、起播超时、迟播与僵尸声均为 0。
- 真 Chromium 以 4× CPU 压力分别注入 18 敌人 + 280 弹体 + 140 待发弹 + 140 爆发、弹体组合压力及 141 敌人灯守怪潮：P95 分别为 18.4/18.6/18.6ms，最慢帧 18.6/33.0/50.2ms。另在 `drawImage` 中强制抛出单帧异常，RAF 继续、战斗时间继续推进、Canvas 基础变换恢复；开发图鉴与普通战斗在后台自动暂停后均可正常退出/恢复，没有复现历史假死。
- production 真浏览器连续 50 轮切后台、冻结、回前台与解除暂停全部恢复；后台墙钟最多只误差 0.04 秒，媒体峰值 12，强制 GC 后堆仅 +0.64 MiB，旁白拒播、起播超时、迟播均为 0，没有发现可见性监听重入或媒体席位累积。
- 宿主仅拒绝 `play()` 而不派发 `error` 的故障注入曾复现三条永久静音路径：环境床、主 BGM 和紧张层各自三次拒播后仍永久复用同一条毒化解码管线；元素音效还会每次只选中池头的坏元素。现按真实播放代次去重失败回调，确实连续失败三次才退池；下一次手势重建后三条循环轨全部续播，元素音效与场景点声则在下一次触发即换池恢复。
- BGM 双元素循环替补若只收到 `play()` 拒绝，旧逻辑会每圈重试同一个坏替补，无感循环永久退化。现在首次拒绝即释放替补、1.2 秒后错峰重建，下一圈成功换岗；替补在后台报错时不抢解码器，回前台后会自动补建。
- 线上 AppID `tteb279d9a740d402e21` 的上一版 `0729-47 预载健壮性加固` 被平台以“互动作品运行过程中存在严重卡顿”拒绝。按评委最可能使用的生产开发面板复现后确认：首章刚开始直接跳到晚年会绕过章节美术硬闸门，`iv-stand:move:3` 缺帧异常随 RAF 逐帧抛出，形成真正的异常风暴与假死。关卡选择现留在可关闭的装订面板内，把目标章提升为最多三路关键解码；完整后才进入，关闭面板会作废迟到自动跳章。延迟 1.6 秒注入下等待、取消、再次进入均通过，零页面/控制台错误。
- 同一路径原会把首章 `narrator-opening` 带进晚年场景。跳章成功前现明确停止旧旁白并清空旧章延迟台词，BGM 仍按 2.1 秒平滑换轨。最终 production UI 依次实跑六章关卡选择，每章收敛到对应单 BGM、最多一条旁白、稳定媒体总量 11（切歌淡化瞬间最多 12）；P95 帧间隔 18.2–18.5ms，最慢 18.7ms，零运行错误。
- 不支持 Pointer Capture 的嵌入式 WebView 故障注入曾复现「拖出画布后摇杆 pointerId 永久占住、角色持续自行移动」：画布 `pointerleave` 现按 pointerId 释放摇杆、命运拖拽、暂停长按和特殊房长按。修复后摇杆立即归零，350ms 后角色坐标不再漂移；原生 touch cancel 后仍能重新长按领取物证、退出暂停并继续结算命运牌。
- `visualViewport.scroll` 事件风暴原会逐次重写根 CSS 尺寸变量，连带触发布局与高 DPR Canvas 背板检查；现按动画帧合并并跳过相同宽高。390×844、DPR 3 下连续注入 500 次 scroll 后重复 CSS 写入为 0，1080×1920 背板没有重建或漂移。
- 快速结束童年时曾能抢在 `chapter-strips.png` 后台装完前进入章节过场，正式绘制随即逐帧抛错；首章战败的 `ending-table.png`、命运纹样与清单外的当铺商人也有同类窗口。三份小型必需 UI 和 1KB 商人现纳入首屏硬门禁，胜利定格纳入暮年章门禁；真浏览器立即清章、连续 11 次审计伤害和立即生成当铺均为 0 页面/控制台错误，封卷画布全不透明。
- 玩家已被下一章美术硬闸门挡住时，缺图解码从后台 1 路提升为关键 3 路；13 个少年章图片请求在 450ms/张的注入延迟下实测峰值并发恰为 3。等待期间章节卡会轮换显示一至三个点的“下一页还在装订”，明确证明主循环仍存活；日常战斗预热仍保持单通道并在战斗中让路，不把优化变成主线程抖动。
- 对同一章必需图片持续注入网络失败时，章末闸门不再每帧重试：实测四次失败间隔约为 0.57/1.01/2.02 秒，随后继续按 4/8 秒封顶退避；等待画面仍正常动画、主循环未卡死，资源恢复后可继续进入战斗。
- 章末美术 Promise 现绑定 `runSerial + encounterIndex`，换局、断点恢复或退回标题都会清掉旧等待态；上一局的迟到回调不再能压缩新局过场或解除新局资源闸门。
- 最终 minify 后的 production 包已从真实 UI 完成“开始呼吸 → 平台 AI 出生 → 跳过漫画 → 选择物证 → 童年战斗”：标题换曲最多 2 路音乐、媒体总驻留最多 12、静音预开嗓虽最多 8 路但真正可闻旁白始终只有 1 路；强制跨过童年 BGM 接缝时双元素换岗后收敛回 1 路，旁白/BGM/环境床同时播放仍为 0 页面错误、0 控制台错误与 0 外网请求。移除 Chromium 的 autoplay 放宽参数后再跑一次仍全部通过，证明首次“开始呼吸”真实手势能恢复标题期被拒的请求轨。
- 最终 production 包在 390×844、DPR 3、4× CPU 冷启动下两次分别于 1.33 秒与 1.06 秒到达 `interactive_ready`；59 份首屏硬门禁美术分别在 0.69/0.47 秒完成。最慢启动长任务 304ms/257ms，均在装帧遮罩内结束，交互开放后没有遗留启动长任务。
- 道具百科恶意标签注入按纯文本显示，注入节点数为 0；production 移动端 smoke 为 0 页面错误、0 控制台错误和 0 外部网络请求。

图标门禁可独立复跑：

```bash
npm run validate:release-icon
```

## 4. 人工复核项

- 320 x 568 窄屏审阅已覆盖封面、出生登记、首章战斗、暂停抽屉与结算四标签：画布 310.5 x 552 居中显示，文档宽高严格保持 320 x 568，无横向或纵向滚动；红章按钮、顶部 HUD、底部一口气栏及暂停按钮均无重叠。证据位于 `output/playwright/narrow-mobile-v2/`。
- 出生请求持续 30 秒仍未返回时，会在同一张档案纸上把等待文案替换为红章“把这一页重新摊开”；不会伪造本地人物，也不会形成无出口死页。窄屏证据为 `output/playwright/narrow-mobile-v2/origin-timeout-320x568.png`。
- Chromium 移动端 production 仿真已通过：缺少 `tt` 时按正典停在可重试出生档案页；注入 `tt.callAIChatCompletion` 成功响应后，可走完封面、声音授权、AI 出生、初始物证选择并进入童年战场，控制台 0 错误。
- production 的 `childhood-room.mp3` 已在首章收到 `206 Partial Content` 并开始加载；四张实机证据位于 `output/playwright/interactive-space-release-v2/`。
- 成年电话链已在 390 x 844 审计画面逐通复核：专用通话面板不再与普通提示、语音字幕重复；第 2 通只显示静默省略号；第 7 通结束后进入病历本页会清除旧面板并播放 `hero-not-busy`。截图位于 `output/playwright/adult-phone-voice-v1/`。
- 原 3 条 ASR 人工复核项已经逐条重选候选、响度归一，并在游戏电话带宽与最终 zip 的 24 kbps 发行副本上复测：`teacher-last-row` 与 `family-dinner-cold` 逐字通过，`landlord-rent-deposit` 仅为“压一负三”的同音转写，三条拼音错误率均为 0。三条源成品均固定 SHA-256 与晋级元数据，严格门禁会阻止重新退回人工复核；提审前仍建议在互动空间真机顺手确认最终听感。
- 必须在互动空间 WebView 中确认首次点击后音频可播放，切后台再回来后环境声能恢复。
- 必须在 390 x 844 与至少一台窄屏 iPhone 上跑完整首局，检查安全区、滑牌、摇杆、暂停、结算与重开。
- 必须确认互动空间后台的 AI 能力开关已开启，模型 Endpoint 与运行时常量一致。

## 5. 平台状态与剩余外部验收

2026-08-01 已通过 `interative_content_mcp.query_game_app_list` 取得权威线上状态：

- AppID：`tteb279d9a740d402e21`
- 状态：3（审核拒绝）
- 拒绝理由：互动作品运行过程中存在严重卡顿，影响正常作品体验
- 被拒版本：`0729-47 预载健壮性加固`，MD5 `39b4f6321b6695ac46411423b0a92538`
- AI 能力：已开启（`AIEnabled: true`）
- 屏幕方向：竖屏

当前候选包尚未上传或覆盖该作品：本轮目标授权了审阅、修复和生成发行包，没有授权直接改变线上草稿或提交审核。下一次平台验收应在用户明确确认后用 MCP 更新同一 AppID，再以真实互动空间 WebView 复核首次手势音频、前后台恢复、完整首局和审核录像中的卡顿路径。线上审核结论必须以更新后的 MCP 返回为准。

## 6. 大赛公开入口复核

- 抖音认证账号“抖音AI创变者计划”当前仍把 `https://aiia.douyin.com/` 标为报名官网；项目对外定位与作品温度、真实社会经验和 AI 创意表达一致。
- 报名站为动态页面，公开抓取未返回可机器核验的本届评分细则；因此技术规格继续以互动空间发布合同和 MCP 返回为准，不根据二手文章臆造赛制。
- 《这一身》的当前适配重点已经落到可审查证据：AI 出生不是装饰入口、六章玩法确实消费生成结果、竖屏完整、离线包无 Web 请求、声音和图标来源可追溯、结局动作由玩家主动完成。
