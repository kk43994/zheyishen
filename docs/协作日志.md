# 《这一身》并行协作日志

本文件供同时操作本仓库的 Codex 窗口追加记录。开始一批工作前先登记范围，完成或放弃后更新状态；不要删除其他窗口的记录，也不要覆盖其他窗口声明中的文件。

## 当前占用

| 时间 | 窗口 / Goal | 状态 | 工作范围 | 主要文件 |
| --- | --- | --- | --- | --- |
| 2026-07-24 07:16 PDT | `019f9171-39d9-7f12-9e28-cf6322b8d709` | 进行中 | 74 件道具美术审查；当前只处理四向上身与真实触发态，已锁定图标和裂纹眼镜；审查页验收、百科美术馆同步 | `scripts/image2/`、`scripts/process_item_image2_assets.py`、`scripts/process_item_state_overlays.py`、`scripts/validate_item_image2_assets.py`、`src/assets/items/state-overlays.*`、`src/hero-item-mutations.ts`、`src/hero-pixel.ts`、`src/item-art-review.*`、`item-art-review.html`、`docs/这一身百科.html`、`scripts/build_wiki_art_gallery.py` |
| 2026-07-24 07:20 PDT | `audit_items_01_25` 子代理 | 已完成 | 01–25 审查完成；12 工资信封 v1 判退、v2 通过，正式接入由主窗口完成 | `output/art-audit-loop/agent-01-25.md`、`scripts/image2/art-loop-v1/12-first-salary/`、`output/imagegen/zhe-yi-shen-art-loop-v1/12-first-salary/` |
| 2026-07-24 07:20 PDT | `audit_items_26_50` 子代理 | 已完成 | 26–50 审查完成；44 第三颗药双相与 37 自动续费四阶段均生成、自审通过，正式接入由主窗口完成 | `output/art-audit-loop/agent-26-50.md`、`scripts/image2/art-loop-v1/44-third-pill/`、`scripts/image2/art-loop-v1/37-auto-renew/` |
| 2026-07-24 07:20 PDT | `audit_items_51_74` 子代理 | Image2 生成中 | 51–74 审查完成；在 `dmitkelao` 生成 55 验孕棒手持实体与孩子影子候选 | `output/art-audit-loop/agent-51-74.md`、`scripts/image2/art-loop-v1/55-pregnancy-test/` |
| 2026-07-25 01:55 PDT | `019f9171-39d9-7f12-9e28-cf6322b8d709` · 新敌人美术阶段 | 已完成 | 六章新增 18 个敌人身份 / 20 套阶段资源；正式图集、Boss 阶段路由、百科详情与桌面/手机验收均已闭环 | `scripts/image2/enemy-roster-v1/`、`src/enemy-pixel.ts`、`src/assets/enemies/`、`scripts/embed_wiki_beast_sprites.py`、`scripts/validate_runtime_art.py`、`scripts/validate_wiki_runtime.mjs`、`docs/这一身百科.html`、`docs/enemy-portraits-v1/` |

## 避让范围

- 本窗口不修改音频、配音、敌人/Boss、关卡、存档、战斗数值与主流程逻辑，除非道具美术接入的最小改动确实需要。
- 工作树在本轮开始时已有大量未提交修改；这些修改均视为其他窗口或用户所有，不回退、不格式化、不批量重写。
- 如果其他窗口需要修改“当前占用”中的文件，请先在本日志追加冲突说明，并尽量把工作拆到不重叠文件。

## 进度记录

### 2026-07-25 01:55 PDT · 新增敌人美术进入确认前冻结阶段

- 用户要求全部候选资源完成并交付确认后，才能替换进游戏文件；本阶段禁止修改正式敌人图集、`src/enemy-pixel.ts` 与百科正式引用。
- 已确认三台独立落地机 `landing001`、`landing002`、`landing003` 均有 `/root/imagegen-pipeline`，从 DMIT 控制机分发互不重叠的 Image2 批次。
- 本批目标为 18 个新增敌人身份、20 套阶段源图：`praise-chair` 与 `ringing-phone` 各拆两阶段；所有候选必须完成语义目检、透明化、像素化和动作图集技术校验后再进入用户审核板。

### 2026-07-25 03:02 PDT · 三台跑图与确认包完成

- `landing001` 完成 A 批 6 套，`others-paper` 与 `wet-shoes` 在语义退回后采用 v2；`landing002` 完成 B 批 7 套，`id-scanner` 与 `task-revise` 采用 v2；`landing003` 完成 C 批 7 套，全部 v1 通过。
- 20 张 `*-selected.png` 均保持 API 返回的 1254×1254 原始 PNG 字节；绿/洋红色键只在审核候选处理时移除一次，避免预缩放和颜色失真。
- 已生成 20 张透明动作候选图集、20 张五态预览、两张总审图和网页审查入口。受击态已移除无语义的白色斜杠，只保留红闪与一像素位移。
- `validate_candidates.py` 全量通过：20/20 源图哈希一致、图集尺寸正确、二值透明、透明 RGB 清零、动作格完整、两张总审图尺寸正确。
- 当前仍为 `promotionAllowed=false`；没有替换正式游戏敌人图集、运行时引用或百科内容，等待用户逐项确认。
- 确认前追加 `integration-plan.json`，逐项冻结 20 个候选到 18 个运行时身份的阶段路由、正式目标路径、当前错误代理和计划显示尺寸；尚未执行该计划。
- 新增两张实际世界尺寸预审板，与当前 35×49 主角同屏比较。首轮发现鞋、门、台灯、识别机默认比例不合理，已在计划层调整为 72/80/40/64px；第二轮目检通过。确认页现含 20 卡、42 张图片与两张尺寸板，桌面双列和手机单列均无横向溢出。

### 2026-07-25 03:30 PDT · 用户批准正式替换

- 用户已在完整确认页看过资源卡、动作预览与世界尺寸板，并明确回复“这些可以确认替换”。`promotionAllowed=false` 的人工门禁解除，开始正式接入。
- 本窗口现在占用 `src/enemy-pixel.ts`、20 张新增敌人正式图集、怪物百科画像/卡片及对应资源门禁；继续避让其他窗口的道具、声音、场景和战斗数值修改。

### 2026-07-25 04:22 PDT · 新敌人正式接入与百科详情闭环

- 20 套批准图集已逐字节提升到 `src/assets/enemies/`，18 个运行时身份全部改用独立资源；`praise-chair` 与 `ringing-phone` 均按 `EnemyUnit.phase` 路由两阶段图集，旧代理映射和占位名单已清空。
- 百科已生成 43 个怪物身份画像与 4 个 Boss 阶段画像；43 张怪物卡全部支持点击/键盘打开“叙事外观档案”，详情中放大形象、机制、反讽与两阶段图。六章编排表补入移动端局部滚动容器，390px 页面横向溢出由 580px 清零至 390px。
- 实机逐个拉起 16 个普通/精英新形象及 4 个 Boss 阶段，桌面与手机均无绿幕残留、旧代理、透明边污染或 UI 越界；游戏和百科控制台均为 0 error。验收图见 `output/playwright/enemy-roster-runtime-contact-sheet.png`、`enemy-roster-mobile-contact-sheet.png`、`wiki-beast-detail-praise-chair-desktop.png`、`wiki-beast-detail-ringing-phone-mobile.png`。
- `npm run build`、`npm run validate:art`、`npm run validate:wiki`、`npm run validate`、`validate_candidates.py`、`validate_promotion_readiness.py` 与 `git diff --check` 全部通过。新敌人美术范围释放，其他窗口可基于本批正式资源继续工作。

### 2026-07-24 07:16 PDT · 道具美术循环开始

- Goal：按百科规范逐件审查视觉表达，使用 DMIT 管线机调用 Image2 修补高优先级缺口，处理入库后同步设计百科并反复复核。
- 已完成的前置修正：漂粉、修眉刀、交掉的闪现、外卖里吃出的头发四个独立图标已入正式图集；绿幕残色处理已修复；百科美术馆已重新嵌入并逐像素核对。
- 下一步：生成审查清单，声明首批待修资产后再调用管线。

### 2026-07-24 07:43 PDT · 管线节点盘点与首批分派

- SSH 别名去重后可用的 Linux Image2 节点为 2 台：`dmitkelao` / `kelao` 指向同一节点，`kelaobwh` / `safe-pony-1` 指向同一节点。
- `pckk` 为 Windows 节点，当前无 `/root/imagegen-pipeline`，不纳入本批生成。
- 凭据只在远端通过 `/etc/sub2api/sub2api.env` 查询 sub2 主池活跃 key；禁止打印、复制或落盘到本地。
- 实现审计发现：部分 `image2-palette-body-mutation` 仅消费生成图色板，角色上的形状由 `src/hero-item-mutations.ts` 程序化绘制。此类修复必须同时验收 Image2 源与运行时形状，不允许只换色板。

### 2026-07-24 07:52 PDT · 74 件分段审查完成

- 报告：`output/art-audit-loop/agent-01-25.md`、`agent-26-50.md`、`agent-51-74.md`。
- 01–25 高优先级：12 工资信封、09 裂眼镜、18 慢手表、22 断脊、04 石头书包图标。
- 26–50 高优先级：41 离婚协议、44 第三颗药、37 自动续费、33 三天可见、40 孩子他爸图标。
- 51–74 高优先级：55 验孕棒、60 momo 头像、63 晚安两小时、67 眼保健操、71 关服那天。
- 两张正式图集扫描的可见键绿残留为 0；本轮需修的是语义和实装尺寸，不是再次全局去绿。
- 12 工资信封 v1 因大红、竖向、像红包/文件袋而判退；已要求 v2 改为横向、灰黄牛皮纸、小钱币边与被攥皱角。

### 2026-07-24 09:08 PDT · 首批三件完成运行时闭环

- 12「第一次工资的信封」：采用 v2 横向牛皮纸信封，已写入正式四向装备图集与独立图标图集；浏览器四向/动作卡验收通过。
- 44「第三颗药」：运行时新增 Image2 双相状态层，红色膨胀对应狂躁、青色收缩对应回落，并以真实玩法相位驱动；浏览器验收通过。
- 37「自动续费到明年的会员」：移除角色旁旧的程序化巨型收据，接入 Image2 四阶段贴地账单；开场 15 秒从 1 段增长到 4 段并绘制在脚后，随后收起，会员金边触发仍保留。
- 正式状态图集升级为多道具多阶段结构：`src/assets/items/state-overlays.png` / `.json`。`npm run build` 与 `npm run validate:item-image2` 均通过，74 件均有运行时 Image2 消费记录。
- 浏览器审查页 `auto-renew` 数据：消费者 `image2-state-overlay-atlas`，四向像素差 `196/252/192/220`，动作四阶段差 `196/656/656/656`，控制台无错误；截图落盘 `output/art-audit-loop/runtime-integration/auto-renew-card.png`。

### 2026-07-24 09:34 PDT · 冰柜接入与呵气通道纠偏

- 70「小卖部的冰柜」保留原 Image2 源，只修缩放、方向与挂点。v4 已接入 `state-overlays`：四向均与人物留 2 个透明逻辑像素，柜底与脚底对齐，左右向拖在人物朝向反侧；旧的程序化贴背大冰柜已删除。
- 冰柜浏览器审查：消费者 `image2-state-overlay-atlas`，四向像素差 `396/412/396/412`，四动作均 `396`；74 件审查页 `valid=true`、控制台无错误。截图：`output/art-audit-loop/runtime-integration/shop-freezer-card.png`。
- 74「冬天呵在玻璃上的字」经用户裁决确认主通道是弹体，不是角色常驻雾团。已删除角色脸部常驻蓝雾和眼部变色，运行时改为 `image2-projectile-trigger-preview`：角色四向/动作变化均为 0，攻击直接显示宽短月白雾锥与 `mist` 轨迹。
- `output/imagegen/zhe-yi-shen-art-loop-v1/70-shop-freezer-74-breath/` 内 74 的 v1-v4 嘴边雾团只保留为错误通道审计证据，`selection.json` 标记 `rejected-as-wrong-channel` / `integrationAllowed=false`，禁止回灌。

### 2026-07-24 09:52 PDT · 书包图标与验孕棒闭环

- 04「装满石头的书包」不重跑上身资源；从已批准 gpt-image-2 正面源重新提取独立图标。新版在 36x36 深色 UI 先读作旧书包，再读到顶部石块和下坠重量，替换旧版黑褐绳网团；对照见 `output/imagegen/zhe-yi-shen-art-loop-v1/04-stone-schoolbag/v1/current-vs-candidate-10x.png`。
- 55「验孕棒」采用 Image2 v1，经三轮 exact 40x56 后处理后批准：验孕棒作为手前层，深紫灰孩子剪影作为脚后层，现有护腹手臂形变和每第三轮跟随弹逻辑保留；旧正式图集的细竖条已停止消费。
- 验孕棒浏览器审查：消费者 `image2-state-overlay-atlas`，四向像素差 `792/756/912/784`，四动作 `792/788/840/788`；74 件审查页 `valid=true`、控制台无错误。截图：`output/art-audit-loop/runtime-integration/pregnancy-test-card.png`。
- `npm run build` 与 `npm run validate:item-image2` 通过；状态图集现含第三颗药、自动续费、冰柜、验孕棒四件正式多层/多阶段 Image2 资产。

### 2026-07-24 17:46 PDT · 后续范围纠正与裂纹眼镜闭环

- 用户明确纠正：本轮后续只优化道具出现在主角身上的四方向形象与触发态，不再新增或修改物品栏图标。此前已完成并批准的图标改动保留，但“孩子他爸图标”等未开始项从队列取消。
- 09「裂了一边的眼镜」的 v1 与第一版 v2 exact 映射均因正面过黑、过满、易读作横带而判退。v2.1 保留 Image2 身份源，只做 exact 40x56 收敛：正面两个断角镜圈、两像素短鼻桥、仅佩戴者左镜三像素裂纹；侧面最长横向连续像素为 2，背面仅两个分离镜腿端点。
- 正式运行时已停止消费旧通用眼镜装备层，改为直接消费 `cracked-glasses:fitted` 状态层；状态图集增至 10 行 / 40 个四方向相位。
- 浏览器 74 卡审查 `ready=true`、`valid=true`。清除重复程序化裂纹后的眼镜四向像素差为 `84/68/8/60`，四动作像素差均为 `84`；截图：`output/art-audit-loop/runtime-integration/cracked-glasses-card-clean.png`。
- `npm run validate:item-image2` 与 `npm run build` 通过。物品栏图标未在本项中修改。
- 41「没有按下发送的离婚协议」Image2 v1 原图已生成，但首次 exact 合成仍像胸牌且背面有不合理白边，主窗口已退回做 v1.1 本地精修，尚未接入正式运行时。
- 60「momo 的头像」在 `kelaobwh` 两次有界请求均被账号池以 `503 No available compatible accounts` 拒绝；无候选原图，未接入旧源冒充完成。节点已恢复原 `inactive/disabled` 状态，失败记录：`output/imagegen/zhe-yi-shen-art-loop-v1/60-momo-avatar/FAILURE.md`。

### 2026-07-24 17:57 PDT · 离婚协议 exact 层闭环

- 41「没有按下发送的离婚协议」采用 Image2 v1 身份源与 v1.1 本地 exact 映射。首次 v1 exact 因正面白块像工牌、背面仍有无物理理由的纸边而判退；v1.1 将纸压在深灰内袋口下，背面完全透明，左右仅保留前胸轮廓相邻的两像素纸边。
- 正式运行时停止消费旧通用刚性纸张，改为 `divorce-draft:fitted` 状态层；方向从候选的 `front/back/left/right` 显式重排为运行时的 `front/left/back/right`。
- 状态图集现为 11 行 / 44 个四方向相位；`npm run validate:item-image2` 与 `npm run build` 通过。
- 浏览器 74 卡审查仍为 `valid=true`。四向像素差 `84/72/0/84`，四动作均 `84`；背面 0 像素为内袋遮挡的正确结果。截图：`output/art-audit-loop/runtime-integration/divorce-draft-card.png`。
- 本项未生成或修改物品栏图标；百科统一重建仍待本批后续项完成。

### 2026-07-24 18:24 PDT · 眼镜确认、慢表/晚安/三日可见闭环

- 用户确认 09「裂了一边的眼镜」当前 v2.1 可以通过；此项锁定，不再继续改动。
- 18「慢了七分钟的手表」Image2 v1 的完整前臂残影因形成“第三只手”判退；v1.1 只批准冻结瞬间的 6–7 像素腕侧短亮点，四动作×四方向均不超过 5×5。常态仍只显示腕表，主效果仍是每七秒弹体悬停与解冻齐射；lag/release 候选禁止接入。
- 63「晚安之后的两小时」v1-v1.3 因侧面手机像竖直终端判退；v1.4 四向均收敛为脚边 7×3 横放手机、4×1 冷白屏和仅 2 像素邻接冷光，已替换旧程序化蓝色大块。
- 正式 `state-overlays` 从 11 行 / 44 格增至 16 行 / 64 格；`npm run validate:item-image2` 与 `npm run build` 均通过。浏览器审查页 74 卡 `valid=true`；截图：`output/art-audit-loop/runtime-integration/slow-watch-card.png`、`goodnight-2h-card.png`。
- 33「朋友圈仅三天可见」重新核对百科后叫停相框 Image2 方案：正式规范是拾取后 3 枚当前配方弹体绕身三圈，不作大件跟随。已删除主角旁常驻双框，审查页改为三弹环绕触发预览，运行消费者改为 `image2-projectile-trigger-preview`；开发计划、主角外观规范、百科、Image2 提示词和旧静态审查脚本已统一为无相框规则。
- 下一批只处理主角/触发态：22 断掉的脊梁骨、60 momo 的头像、67 眼保健操、71 关服那天；继续禁止修改物品栏图标。

### 2026-07-24 19:18 PDT · 四项主角体现与百科同步闭环

- 22「断掉的脊梁骨」采用整个人驼背前倾与贴背旧伤。用户已确认当前驼背无问题，本版锁定；禁止回退到外挂骨柱、骨刺、额外横杠、影子异物或第三肢体。
- 60「momo 的头像」采用 Image2 v2.1：主角直接戴圆头粉色小恐龙头套，安全/受威胁两态均由正式状态图集消费；身体、衣服和四肢不替换，旧矩形头像框已移除。
- 67「眼保健操」采用 Image2 v1：只在 0.5 秒触发窗用部位遮罩移除原双臂，再换成贴眼眶轮刮、揉按的抬手姿态；常态不显示，不使用悬空眼睛、长杆或望远镜表达。
- 71「关服那天」采用 Image2 v1：影子边常驻一个有头身的小伙伴，致命伤依次出现、跃出、替挡、断线并永久清空；旧全屏关机遮罩已停止绘制，不出现屏幕、终端、设备或地面残骸。
- 74 件运行时审查卡已全部落盘，页面状态为 `ready=true`、`valid=true`；百科美术馆同步七张全量分段审查图和四项重点图。以上四项均未修改物品栏图标。

### 2026-07-24 22:16 PDT · 百科道具图鉴卡与详情层

- 「道具志」进入时改为默认展示 74 张紧凑图鉴卡；每张卡直接消费正式 `src/assets/items/icons.png` 图集，展示编号、形象、名称、来源、品质、记忆简介与一句机制摘要。原七组展开式长卡保留为隐藏正典数据源，不再与总览重复占屏。
- 总览提供名称/记忆/效果搜索、来源筛选、品质筛选与清除按钮，初始状态固定 `74/74`；点击任一图鉴卡打开详情层，合并展示记忆、正负效果、主角身上体现、反讽、精确数值、规则、人物体现、弹体、触发反馈与叠加裁决。
- 详情层支持遮罩点击关闭、Esc 关闭、焦点回到原卡片、Tab 焦点约束和页面滚动锁；图标、标题和关闭按钮在手机端不重叠。
- Playwright 桌面验收：4 列卡片、74 张、74 个正典卡同步、10 组旧长卡隐藏、无横向溢出；截图 `output/art-audit-loop/wiki-item-review/wiki-item-catalog-desktop.png`、`wiki-item-detail-broken-spine-desktop.png`、`wiki-item-detail-eye-exercise-desktop.png`。
- Playwright 手机验收：390×844 下稳定 2 列、单卡 171×220、74 张、无横向溢出；Momo 详情明确为圆头粉色小恐龙头套。截图 `output/art-audit-loop/wiki-item-review/wiki-item-catalog-mobile.png`、`wiki-item-detail-momo-mobile.png`。
- `validate_wiki_runtime.mjs` 新增 1–74 连续编号、74 个图集索引一一对应、总览/详情入口、手机双列和详情头结构门禁；不修改任何道具图标内容。

### 2026-07-24 22:38 PDT · 详情卡补齐主角实机体现图

- 从 `item-art-review.html` 当前通过门禁的 74 个运行态画布逐件导出 `480×250` PNG，落盘至 `docs/item-manifestations-v1/`；不复用旧静态草图，也不修改物品栏图标。
- 百科每件道具的详情层在文字字段前直接展示主角四方向、动作与触发态图；图片下保留“主角实机体现”说明，人物规范文字继续作为补充。
- `validate_wiki_runtime.mjs` 新增 74 张文件名、数量、PNG 格式与 `480×250` 尺寸门禁，避免后续详情卡漏图或导出走样。
- Playwright 逐个打开 74 个详情入口，图片均解码为 `480×250` 且无失败；桌面 `1440×1000`、手机 `390×844` 均无横向溢出或标题/关闭按钮遮挡。截图：`output/playwright/wiki-item-detail-manifestation-desktop.png`、`output/playwright/wiki-item-detail-manifestation-mobile.png`。

### 2026-07-24 23:50 PDT · 怪物图鉴 25 张正式肖像全量替换

- 怪物图鉴原有 8 张旧 SVG、11 张内嵌 Base64 缩略图和 6 张错误裁切的整图集引用已全部移除；25 张卡现统一引用 `docs/enemy-portraits-v1/` 的运行时正面待机肖像。
- 肖像直接取自 `enemy-pixel.ts` 实际消费的图集：32 像素普通怪做整数 2 倍放大，48/64 像素精英与 Boss 保持原生像素比例并居中到 `64×64`，不做模糊的非整数缩放。
- `scripts/embed_wiki_beast_sprites.py` 改为可重复生成并逐卡替换；`validate_wiki_runtime.mjs` 新增 25 张文件、尺寸、链接以及旧 SVG/Base64 清零门禁。
- Playwright 桌面检查 25/25 张均已加载、非空且路径唯一；四组卡片逐组目检无错配。手机 `390×844` 下单列卡宽 350、无图片或文字溢出。截图：`output/playwright/wiki-enemy-catalog-desktop.png`、`wiki-enemy-catalog-mobile.png`、`wiki-enemy-group-1.png` 至 `wiki-enemy-group-4.png`。

### 2026-07-25 00:36 PDT · 叙事外观档案补齐与角色外描边移除

- 「七十四件 · 叙事外观档案」原先只有 21 张旧 SVG，其余 53 张没有图标节点；现已按规范化名称逐卡匹配 `icons.json`，74 张长卡全部消费正式 `icons.png` 图集，旧 SVG 清零。卡片顺序与图集索引不同的条目按 `data-item-id` 和真实索引关联，不按页面顺序误套。
- 新增 `scripts/sync_wiki_item_archive_icons.py` 可重复同步脚本与百科门禁；`build_equipment_art_contract.mjs` 同步兼容带索引属性的档案卡。Playwright 主动展开默认隐藏的档案本体，桌面 74/74 图标尺寸与背景位置正确，手机卡宽 350、无溢出。截图：`output/playwright/wiki-narrative-archive-icons-desktop.png`、`wiki-narrative-archive-icons-mobile.png`。
- 删除 `PixelHeroRenderer` 每帧额外生成的浅米色膨胀轮廓（旧色 `#d8cbb5`、透明度 58%）；只保留角色贴图内部深墨像素边与脚底阴影。`validate_runtime_art.py` 新增浅色外描边回归门禁，百科正典同步改为“不再额外套浅色外描边”。
- 审查页 74 张运行态画布均通过 `ready=true`、`valid=true`，并已重新导出覆盖 `docs/item-manifestations-v1/` 的全部 `480×250` 详情图。对比截图：`output/playwright/hero-no-outer-outline-review.png`；手机详情截图：`wiki-item-detail-no-outer-outline-mobile.png`。

### 2026-07-25 01:18 PDT · 「对方正在输入…」改为三拍散射攻击

- 本项修改的是攻击方式与主角触发态，不修改物品栏图标。旧手机、聊天气泡、常驻雾团、1.2 秒蓄力巨弹、缩成“嗯”与 KTV 合并坍缩演出全部停止消费。
- 普通自动连射改为固定三拍：有敌人时头顶每 0.5 秒增加一个像素句号；第三个句号出现时立即释放 12 向 360 度大范围散射，并停留 0.5 秒保证可读，随后开始下一轮。
- 十二枚散射弹严格等角，不受额外弹数或《五个哈》继续倍增；每枚继承当前《一口气》的外形、材质、穿透、追踪、折返、分裂与爆炸。KTV 六秒声浪保留为独立周期。
- 运行时美术清单已改为 `effect / transient / image2-projectile-trigger-preview`，百科详情人物图已重新按原始 `480×250` 画布落盘：`docs/item-manifestations-v1/58-typing-indicator.png`。审查卡与百科详情均明确“不举手机、不挂气泡”。
- Playwright 实机采样依次得到 `1 点 / 2 点 / 3 点`；第三态散射轮次递增并生成 12 枚弹，方向角为 `0 / ±0.524 / ±1.047 / ±1.571 / ±2.094 / ±2.618 / 3.142`。截图：`output/playwright/typing-indicator-manifestation-card-final.png`、`wiki-typing-indicator-detail.png`、`typing-indicator-radial-attack-final.png`。
- TypeScript、生产构建、道具合同、道具运行机制、35 类弹体覆盖、74 件 Image2 消费与 `git diff --check` 均通过。百科总门禁与总美术门禁仍被另一窗口尚未闭环的怪物肖像/条目映射（`coat-rack`、`wet-shoes`、`revolving-lantern`、`praise-chair`）拦住；未跨范围覆盖对方改动。

### 2026-07-25 05:50 PDT · P2 剧情道具五件与固定掉落传承线落地

- 第五档「这一身」入库：`relics.ts` 三件新道具（入学通知书 / iPhone 17 Pro Max / 父亲的病历本），雨衣与工牌改第五档并移出全部随机池；道具总数 74 → 77。品质罗马字全部扩到 Ⅴ。
- 固定掉落流：每章大 Boss 死亡先进整屏 `storyDrop`（拾取即穿戴、不可拒绝、含章节含义行与「不进三选一」印），点一下后接回正常 Boss 三选一。暮年不掉，开始还。
- 机制细节：通知书=四选一+物证台倒计时冻结（不能不选）；iPhone=伤+30%·攻速+10%·拾取当下零钱清零·每 15s 消息断攻 0.4s（三拍散射节拍同停）；病历本=`permanentHpLost` 每 10 点伤+8% 无上限（只记 changeMaxHp 真实生效部分，回溯累计，进存档）+治疗-40%；收灯人剥离加「雨衣最后被剥」保底。
- 门禁：生产合同补三份（来源新增「传承」），relic-contracts/wiki/art 计数改 77；三件新道具的图标、档案卡、480×250 体现图列入 PENDING_ART 豁免清单——**batch19 美术落地后请从 validate_wiki_runtime.mjs / validate_runtime_art.py 的清单移除并补 icons.json 映射（索引 74/75/76）**。图标描述务必逐条对照 flavor（书包漏石头教训）。
- 调试钩子：新增 `defeat-boss`（先压 hp=1 再补刀，绕过沉默的父亲每击减伤壳）；`equip` 改走真实 `acquireItem` 路径（修「直接赋值 this.items 不触发 combo/永久代价」老坑；注意重复 equip 同名道具会重复记永久损失，属 dev 工具已知行为）。
- 验证：tsc 0 错；build ✓；relic-contracts / relic-runtime / projectiles / scene / materials / wiki / art / mobile 八项门禁全绿；Playwright 实机冒烟四链路全过（童年通知书→四选一、青年零钱 12→0 与 0.655 间隔、少年双阶段父亲→雨衣 0.850 间隔、病历本 28 点损失→×1.16），pageerrors 0。
- 本批未动：图标图集、体现图、百科档案卡与美术馆（待 batch19 后统一重建 rsync）。

### 2026-07-26 01:15 PDT · 全门禁矩阵认证 + 百科 77 版上线

- 隔夜第八至十五轮之后做整仓健康认证：tsc 0 错；26 项 validate 全绿（voice:strict 51/51 含 1 条既有 teacher-last-row 人工复核项、sound 16/16、release-icon/budget/metadata 走 `npm run package` 正规流程后通过）。
- 修正一个流程性坑：裸跑 `npm run build` 会把 WAV 母版/voice-concepts/favicon 留在 dist 触发 release-budget 红——发布产物一律走 `npm run package`（构建→剥离→打包→自动同步验收文档指纹）。新 zip 5,519,130 字节 / SHA e80789e2…，余量 1.78 MiB。
- 百科 77 件版部署上线（scripts/deploy_wiki.sh，v=202607260107）：线上实测物证墙 77、图鉴卡 77、第五档 Ⅴ 筛选可用、Boss 美术馆逐帧节在线，控制台 0 错误。
- 剩余事项全部为人工/外部：真机 WebView 音频与完整首局复核、teacher-last-row 听感、互动空间后台 AI 开关确认、`interative_content_mcp` 四个工具仍未加载（上传与审核状态查询被阻塞）。
