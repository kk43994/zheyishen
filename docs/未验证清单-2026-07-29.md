# 未验证清单 · 2026-07-29

这一天的改动绝大多数只过了 `tsc --noEmit` 与打包门禁，**没有跑过完整对局**。
生产包在浏览器里出生必失败（审核红线设计），Claude 侧进不了完整战斗，
所以下面这些只能靠真机确认。

分两栏：**已验证**（有证据）与**未验证**（无证据）。混在一起会让清单失去信噪比。

---

## 已验证 · 有明确证据

| 项 | 证据 |
|---|---|
| AI 平台调用修复（流式 + 关思考 + 超时） | 用户真机确认「现在可以正常调用了」 |
| 顿帧修复（音效改 Web Audio） | 用户真机确认「现在不卡了」 |
| 打击音效仍在（不是靠丢声音换的流畅） | 用户真机确认 |
| 颗粒纹理烘焙逐像素相同 | 浏览器对拍 3 用例 / 230 万颜色通道，差异 0 |
| clip 消除逐像素相同 | 浏览器对拍 48 用例（含分数缩放），差异 0 |
| 留灯间标题像素风居中无框 | 截图确认 |
| HUD 去底框（外号/零钱/武器条） | 截图确认 |
| 图鉴详情页试穿体现 | 用户确认「我看见了」 |
| 生产开发面板「关卡选择」 | 2026-08-01 最终 production UI 六章逐章跳转；P95 18.2–18.5ms、稳定媒体 11（切歌瞬时≤12）、零运行错误；并修复目标章缺图逐帧异常风暴 |
| 历史 8 MiB 上限记录 | 当时上传 8,396,121 字节返回 `code:413`；同日后续提交 `0a989c0` 已把当前平台合同与门禁正式更新为 20 MiB，不再作为现行限制 |
| 命运卡「留下」本来就是 AI 运算 | 代码核实 `validateResponse` → `validateFreeReward`，非运行验证 |

---

## 未验证 · 需要真机

### 最高优先：可能整局玩不下去

- [ ] **跳关 bug 是否真修好**（`a4d7289`）。`startStage()` 漏重置 `stageBossDefeated`，
      导致第二章起开局即跳。修复只做了逻辑推理，没有跑过第二章。
- [ ] **打死 Boss 即结算后每章实际时长**。取消倒计时 + 血量翻倍，两个变更叠加，
      节奏可能严重偏离原设计。
- [x] **开发面板「关卡选择」页**。2026-08-01 已在最终 production UI 逐章实跑六章；
      `startStage()` + `battleTime = bossAt` 路径可达，目标章美术先过硬闸门，旧旁白不再跨章。
- [ ] **开发面板「获取道具」按钮**。从未点过，`acquireItem` 在面板上下文里的表现未知。

### 数值与手感

- [ ] Boss 血量翻倍后各章难度曲线（衣柜 300 / 父亲 780 / 老板椅 1088 / 电话 1520 /
      催收 2024 / 收灯人 2150）。
- [ ] 湿鞋 4049 血 + 速度 18→44 爬升的追击手感；它与电话 Boss 并存是否成立。
- [ ] 收灯人 19999 血**是否真能被打死**；隐藏结局能否触发。
- [ ] 传承机制解锁链路：`keeperSlain` → 留灯间「上一世留下的」是否正确上锁/解锁。

### AI

- [ ] **人生封卷能否在收灯流程走完前就绪**（收灯人出现即后台开写）。
- [ ] 封卷的**文本质量**：是否真把出身、外号由来、每张命运牌串成故事，
      而不是罗列；是否触碰了「不得新增人物/道具/往事」的红线。
- [ ] 结局页「这一生」页签的版面（长文 200 字在 320 宽下的实际行数）。
- [ ] 页签从 4 个加到 5 个后的点击命中（几何已改为从 `RESULT_TABS.length` 推导，未实测）。

### 交互与表现

- [ ] 电话接听说明字幕「电话在响。得走到跟前，站满三秒。」的出现时机与可读性。
- [ ] 「你怎么看」常驻落脚圈 + 说明字幕「他在等一个回答。站进那个圈里。」。
- [ ] 咽下/吐出点击命中（`0f1ef00`，代码接上但没在真机点过）。
- [ ] 钥匙爆炸三层粒子的实际观感。
- [ ] 飘字 1.1 秒 + 描边后是否仍会互相叠字（战斗密集时）。

### 音频（0729-9 ~ 0729-15 那批）

- [ ] 性能面板「音效引擎」那行的实际读数：`running` / `suspended` / `unavailable`、
      `base`/`out` 延迟毫秒、`hit裁50ms`、`语音滤波N`。**这行从来没被看到过。**
- [ ] 7 种语音滤镜是否真的接上（`createMediaElementSource` 在抖音 WebView 里能否用）。
- [ ] 环境音/配乐「被宿主暂停后续播」修复是否生效。

---

## 已知未修

- `validate:core` 缺 `LAMP_STRIP_TO_RELEASE_DELAY` / `LAMP_RELEASE_CONFIRM_DELAY`
  的 gameplay proof。用 `git stash` 对照确认过是**隔夜遗留**，非 2026-07-29 引入。
- BGM 每首仅 18 秒 / 32kbps，环境音每条仅 8 秒 —— 循环感来自素材本身，非代码。
  用户裁决「短的可以重复，问题是种类太少」，待补素材（阻塞于 heygen OAuth）。
- 门禁通病：多处合同**守实现而不是守意图**，调参就会撞红。今天撞了四次
  （`stream:false`、超时常量、模型 ID、Boss spec 整行、`ctx.fillText(nickname)`）。
